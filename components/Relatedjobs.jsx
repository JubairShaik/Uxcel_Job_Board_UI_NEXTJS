import React from "react";
import { related_jobs } from "@/constants";

const Relatedjobs = () => {
  return (
    <div>
      {/* {related_jobs.map((item) => (
        <h1>{item?.company}</h1>
      ))} */}
    </div>
  );
};

export default Relatedjobs;
